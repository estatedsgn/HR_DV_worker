# Журнал изменений агента (хронология)

Сюда пишутся ВСЕ изменения системы с датой/временем (МСК), причиной и эффектом.
Новые записи — СВЕРХУ.

---

## 2026-06-10 ~21:45 МСК — Конверсионный пакет №1 (локальная сессия Claude)

Контекст: полное исследование живой системы (50 лидов в БД сервера, все переписки
прочитаны). Воронка теряет лидов в трёх местах:

1. **74% лидов (37/50) застряли на interest_check**, из них ~29 получили опенер,
   не ответили — и больше им НИКТО НИКОГДА не писал. Лиды просто умирают.
2. **Лишний гейт согласия про ЗП**: после «интересно» и возраста бот ЕЩЁ РАЗ
   спрашивает «давай расскажу про зп и график?» — 5 лидов умерли ровно на этом
   вопросе (девочка уже сказала «да», зачем спрашивать снова).
3. **«Да» на вопрос «тебе уже есть 18?» не распознаётся** — бот тупо переспрашивает
   «сколько тебе лет?», выглядит роботом (реальный кейс: лид 2c3224b1 ответила
   «Да» и получила тот же вопрос ещё 2 раза).

Изменения этого пакета:

### 1) Лестница повторных касаний для замолчавших лидов (re-engagement)
- Новый сервис `app/services/funnel_graph/reengage.py`: если последнее сообщение
  в диалоге — наше и лид молчит, шлём до 3 «бампов» с нарастающими паузами
  (по умолчанию 4ч → 20ч → 48ч), потом навсегда замолкаем.
- Тексты разные для «холодных» (вообще не ответила на опенер) и «тёплых»
  (переписка была, затихла). Для тёплых — переформулированный вопрос текущей
  стадии (через QUESTION_VARIANTS), не дословный повтор.
- Защиты: не бампим, если есть неотправленные джобы в очереди; идемпотентность
  `reengage:{dialog}:{n}`; лимит на цикл; только в рабочие часы (гейт автопилота);
  терминальные стадии и пауза «до 18 лет» исключены.
- Включение: `REENGAGE_ENABLED` (вкл по умолчанию), интервалы настраиваются.
- Интеграция: `scripts/run_autopilot.py` запускает скан раз в ~5 минут.

### 2) Убран гейт salary_schedule_offer из основного пути
- Было: age_check → голосовые о работе → ВОПРОС «давай расскажу про зп?» → ждём
  «да» → голосовые про ЗП → «остались вопросики?». Каждый лишний вопрос = потеря
  части лидов (классика воронок).
- Стало: age_check → мостик-предупреждение → голосовые о работе + голосовые про
  ЗП/график ОДНИМ паком → текстовый дайджест условий → «остались вопросики?».
- `action_executor` теперь умеет проходить ЦЕПОЧКУ action-стадий за один ход.
- Стадия salary_schedule_offer оставлена в политике для обратной совместимости
  (5 живых лидов сейчас стоят на ней — их выведет существующая логика
  offer_deliver_voices).

### 3) Текстовый дайджест после голосовых (новый шаблон salary_digest_message)
- После голосовых уходит короткий текст: выплаты на РФ-карту еженедельно (на
  стажировке — после смены), график гибкий. Если девочка не слушает голосовые —
  инфа всё равно дойдёт. Цифры намеренно НЕ называем (политика: конкретика на
  собесе).

### 4) Подтверждение «18+» словами
- `semantic.py`: на age_check утвердительный ответ («да», «конечно», «есть»)
  засчитывается как 18+ ЕСЛИ наш последний вопрос упоминал «18». Возраст пишем
  как 18, на собесе всё равно проверяют документы.

---

## 2026-06-10 18:51 МСК+1 — Фиксы дублей и повторов (серверная сессия Claude)

Коммиты b9021df, 5c0be8d, ef03040 (задеплоены, процессы перезапущены 18:51):
- **Каждое исходящее уходило ДВАЖДЫ** (1–6 мин между копиями): починены
  readback-дубли поллинга (`adopt_outbound_readback`), схлопывание истории для
  LLM, жёсткий запрет слать текст, совпадающий с недавним исходящим
  (`dedupe_against_recent_outbound`).
- Промпт: возражения отрабатываются по существу (1 раз), запрет повторов,
  разумная пакетная отправка.
- Свайпер: пагинация истории Дайвинчика — не теряем мэтчи из больших пачек.
- Активные часы автопилота, функциональный вотчдог, дедуп экспорта чатов.

Проверено по БД: после рестарта 18:51 новых дублей не создавалось.

---

## Известные проблемы (бэклог)

- Сожжённый горячий лид 783cc778: записалась на собес 10.06 15:00 (есть карточка),
  но старая очередь потом снова спросила возраст. Runtime стоит на age_check.
  Корневая причина (флаш протухшей очереди) закрыта фиксами 06-10; саму девочку
  стоит руками передать менеджеру.
- Воронка длинная: после вопросов идут profile_theme → room → phone → офер →
  контакты → день → время. Кандидат на следующее упрощение, если конверсия
  mid-funnel не вырастет.
- `lead_intake` помечает интейк failed, если нет синка аккаунтов (см. memory
  fresh-db-bootstrap-gotchas) — лечится `reprocess_failed_intakes.py`.
